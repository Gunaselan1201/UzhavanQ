PORT=
MONGODB_URI=
JWT_SECRET=
# All slot/date logic assumes the server's own local clock is IST (see
# server/utils/timeSlots.js) — must be set explicitly wherever the host
# doesn't default to it (e.g. Render defaults to UTC).
TZ=Asia/Kolkata
